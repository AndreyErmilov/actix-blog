# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
with_docs = true

import_types = [
    "crate::diesel::sql_types::*",
    "crate::exports::Article_status",
]
